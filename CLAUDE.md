# 온더브라이드 — 작업 규칙

대표가 정한 것과, 실제로 사고가 났던 자리를 적어 둔다.
**이 저장소는 공개(public)다.** 고객 이름·연락처가 든 것은 절대 커밋하지 않는다.

---

## 올리기 전에 반드시 보는 것 (2026-08-28 대표 지시)

> 「뭐가 자꾸 문제가 생기는 느낌이네?」 — 하루에 18번 올렸고, 그중 셋을 대표가 먼저 발견했다.
> 셋 다 같은 이유였다: **컴퓨터 화면만 머리에 두고 만들었다.**

배포 전에 아래를 스스로 확인한다. 하나라도 안 봤으면 올리지 않는다.

1. **폰에서 어떻게 보이나**
   대표는 거의 폰으로 본다. 작가들도 전부 폰이다.
   - 칸이 여럿인 줄은 좁은 화면에서 **어디서 접히는지** 본다.
     (열두 칸을 한 flex 에 두면 아무 데서나 접힌다 — 묶어서 줄을 정해준다)
   - `title=` 은 폰에서 **안 뜬다.** 손가락을 올려두는 동작이 없다.
     설명이 필요하면 눌러서도 보이게 한다.
   - 이미 있는 `@media` 규칙을 고쳤으면 그 규칙을 보던 시험도 같이 고친다.

2. **대표가 쓰면 숫자가 틀어지는가**
   대표는 관리자이면서 작가이기도 하다. 대표의 사용이 집계에 섞이면 그 지표는 통째로 죽는다.
   - 접속·조회·응답 같은 것을 셀 때 **대표가 확인하러 들어온 것을 빼는 길**을 같이 만든다.
   - 관리자 화면에서 여는 링크에는 표시를 달고(`adm=1`), 관리자로 로그인된 기기도 뺀다.
   - 작가·신부에게 나가는 링크에는 그 표시가 붙으면 안 된다 (붙으면 아무도 안 세어진다).

3. **누르면 어떻게 되는지를 보여주는가**
   화살표·아이콘은 **지금 상태**가 아니라 **누르면 벌어질 일**을 가리킨다.
   접혀 있으면 ∨(누르면 펴진다), 펴져 있으면 ∧(누르면 접힌다).

4. **한 번에 하나씩**
   20분에 하나씩 올리면 대표가 확인할 틈이 없다.
   큰 것 하나를 끝내고 보여드린 뒤 다음으로 간다.

---

## 배포·검증

- `main` 에 푸시하면 Vercel 이 올린다. **라이브는 onthebride.com** (`.co.kr` 아님).
- 이 PC 는 브라우저·외부 DNS 가 막혀 있다. 확인은 이렇게 한다.
  - 배포 상태: `gh api repos/onthebride/OTB_home/commits/<sha>/status`
  - 내용: `curl` 로 라이브 파일을 받아 도장(`?v=`)이 로컬과 같은지, 바뀐 코드가 실려 있는지 본다
- HTML 을 고쳤으면 `node tools/stamp.mjs` 로 도장을 다시 찍는다.

## 시험

- **전체 돌리기: `cd .backups/tests && node sweep.mjs`**
  ⚠ 스크래치패드(임시 폴더)에서 돌리지 않는다. 거기는 **청소되면 통째로 사라진다** —
  2026-08-28 에 아홉 개, 2026-08-30 에 아홉 개가 날아갔다. `pgrun/node_modules` 까지
  지워져 DB 시험 전부가 조용히 죽은 적도 있다.
  `.backups/` 는 gitignore 라 공개 저장소에 안 올라가고, 저장소 안이라 안 지워진다.
- **PASS 총합을 매번 본다.** 세 가지가 다 신호다.
  · `FAIL 0` 인데 `rc 1` — 중간에서 터진 것이다
    (소스를 잘라와 돌리는 시험이 많아, 잘라온 구간 밖의 이름을 쓰면 부를 때 터진다)
  · PASS 수가 **줄었는데 FAIL 이 0** — 시험 파일이 사라졌거나 **옛 판으로 덮였다.**
    2026-08-30 에 `analytics.test.mjs` 가 8/26 판(28개)으로 돌아가 있어 25개를
    조용히 안 돌리고 있었다. 아무 데서도 FAIL 이 안 났다
  · 파일 수가 줄었다 — 위와 같다
- 새 시험을 만들거나 고치면 **그 자리에서** `.backups/tests/` 에 넣는다.
  DB 시험은 `.backups/tests/pgrun/` 이다.

## 절대 규칙 — 예약과 배정 (2026-08-29 대표 지시)

> 「스케줄 절대 지워지면 안됨 / 배정된거 내가 수정하는게아니면 절대 없어지면 안됨」

이 둘은 다른 어떤 것보다 앞선다. 화면이 좀 못생겨도, 느려도, 이건 지킨다.

1. **예약(`public.bookings`)은 지우지 않는다.**
   손님이 취소해도 `status = '취소'` 로 둘 뿐 줄을 지우지 않는다.
   내가 SQL 로 정리·시험을 할 때도 `delete from public.bookings` 를 운영에서 실행하지 않는다.
   시험은 반드시 트랜잭션 안에서 하고 `rollback` 한다.

2. **배정(`assignee_id`·`sub_assignee_id`)은 대표가 바꿀 때만 바뀐다.**
   자동으로 비우는 코드를 만들지 않는다. 배정을 건드리는 일괄 작업은
   먼저 대표에게 무엇이 어떻게 바뀌는지 보이고 허락을 받는다.

3. **감시 장치를 끄거나 우회하지 않는다.**
   - `trg_assignment_audit_upd`·`trg_assignment_audit_del` — 배정이 바뀌면 누가 바꾸든 기록이 남는다
   - `private.assignment_health_check()` — 한 시간마다 셈이 줄지 않았나 본다
   - `private.booking_backup_run()` — 낮 12시에 예약·배정을 통째로 떠 두고,
     어제 것과 견주어 **감사 기록으로 설명되지 않는 차이**가 있으면 대표에게 알린다
   트리거를 잠시 끄고 작업해야 하면 대표에게 먼저 말한다.

4. **헛경보를 그냥 두지 않는다.**
   2026-08-30 에 「배정 5건 줄어듦」이 울렸는데 예식이 지나간 것뿐이었다.
   헛경보가 반복되면 진짜 경보를 안 믿게 된다. 원인을 찾아 그날 고친다.

되살리는 법 (사고가 났을 때)
```sql
-- 그날 백업으로 배정만 되돌린다
update public.bookings b set assignee_id = k.assignee_id, sub_assignee_id = k.sub_assignee_id
from private.booking_backup k where k.taken_on = date '2026-08-30' and k.booking_id = b.id;
-- 사라진 예약이 무엇이었는지 본다
select * from private.booking_backup k where k.taken_on = date '2026-08-30'
 and not exists (select 1 from public.bookings b where b.id = k.booking_id);
```

## 손대면 안 되는 것

- `.backups/` 와 셀렉 파일 폴더는 **절대 지우지 않는다.**
- 자동 셀렉 결과는 「2026 자동셀렉」에 들어간다.
- 배정 자동 추천은 대표가 «자리를 잡고 할 예정» 이라 했다. 먼저 꺼내지 않는다.

## 자잘하지만 매번 걸리는 것

- `heredoc`(`cat <<'EOF'`)은 **역슬래시를 먹는다.** 정규식이 든 패치 스크립트는 Write 도구로 만든다.
- `admin.html` 은 줄끝이 CRLF 다. 잘라 붙일 때 `\r\r\n` 이 생기지 않게 한다.
- 같은 고르개를 CSS 에 두 번 쓰지 않는다 — 시험의 `rule()` 이 앞엣것만 본다.
- 화면 하나에 `.sub-tab` 같은 묶음이 여럿이면 `querySelector('.sub-tab')` 로 통째로 찾지 않는다.
  제 상자 안에서만 찾는다 (`#tab-settings .sub-tab`).
- 함수에 칸을 더할 때는 `drop function if exists` 를 같이 쓴다.
- DB 를 진짜로 쓰는 시험은 **진짜 자료와 부딪힌다.** 「아무것도 없다」를 전체 수로 재지 말고,
  내가 만든 것만 골라 보거나 전후를 비교한다.
- **「입금」이 두 가지다** (2026-09-03).
  · `deposit_paid`·`balance_paid` — **손님이 우리에게** 넣는 계약금·잔금. 홈 「💳 미입금」
  · `main_pay_at`·`sub_pay_at` — **우리가 작가에게** 주는 작가비. 홈 「💸 작가비」
  반대 방향이다. 글자·색을 갈라 적고, 손잡이 이름도 갈라 쓴다 —
  `#tab-dashboard .dl-paid` 는 손님 입금 확인에 **통째로** 걸려 있어서,
  작가비 단추에 `dl-paid` 를 붙이면 누를 때 손님 계약금이 확인되고 **알림톡(F)까지 나간다.**
- 새로 세기 시작하는 것에는 **시작선을 둔다.** 그 앞의 자료는 「없었던 일」이 아니라
  **우리가 안 적어둔 것**이다. 그대로 세면 첫날부터 헛경보가 울린다
  (작가비 55건 · 접속기록 — 대표 «접속기록은 오늘부터 다시 재»).
  ⚠ 다만 **화면과 알림의 시작선은 다를 수 있다.** 화면은 옛것까지 보여야 처리를 하시고,
  알림만 시작선 뒤를 본다.
- `bookings` 는 **모든 UPDATE 에 트리거가 돈다** (`trg_booking_change_notify`).
  칸 하나만 건드려도 도는 자리라, 「안 나갈 것이다」를 눈으로 읽지 말고
  **트랜잭션 안에서 눌러보고 나가는 표를 세어본 뒤 `rollback`** 한다.
